# Ângulos
import math
ang = float(input('Digite o ângulo que deseja: '))
print('O cosseno de {} é {:.2f}'.format(ang,math.cos(ang)))
print('O seno é {:.2f}'.format(math.sin(ang)))
print('A tangente é {:.2f}'.format(math.tan(ang)))